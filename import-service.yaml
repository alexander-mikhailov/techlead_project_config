server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

#management:
#  endpoints:
#    web:
#      exposure:
#        include: refresh, info, health, logfile, loggers, metrics, scheduledtasks, threaddump, heapdump

app:
  scheduling:
    enable: true